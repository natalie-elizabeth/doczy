{
  "name": "nanapi",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "NODE_PATH=/server node ./bin/www",
    "ssh": "docker exec -it $(docker ps | grep docker_api | awk '{ print $1}') bash",
    "migrations": "sequelize db:migrate:undo:all && sequelize db:migrate",
    "start:dev": "NODE_PATH=/server nodemon ./bin/www"
  },
  "dependencies": {
    "bcrypt-nodejs": "^0.0.3",
    "body-parser": "^1.17.2",
    "express": "^4.15.3",
    "morgan": "^1.8.2",
    "pg": "^6.2.4",
    "sequelize": "^4.1.0",
    "sequelize-cli": "^2.7.0"
  }
}